using TMPro;
using UnityEngine;

public class UpdateCapitalUi : MonoBehaviour
{

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    [SerializeField] private GameObject CapitalTXT;
    [SerializeField] string current_value = "0";
    void Start()
    {
        //current_value = CapitalTXT.GetComponent<TextMeshPro>().text;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // DEPRECATEDDDDDDDDDDDDDDDDDDDDDDD DUE TO TIME
}
